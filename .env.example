# MongoDB Connection
MONGODB_URI=mongodb://localhost:27017/gamefinder

# ========================================
# Firebase Admin Configuration (Server-Side)
# ========================================
# Firebase Admin is used for authentication, file uploads, and server-side operations.
# You can configure it using ONE of the following methods (listed by priority):
#
# METHOD 1 (Highest Priority): Service Account JSON String
# --------------------------------------------------------
# Set the entire service account JSON as a single environment variable.
# Download from: Firebase Console → Project Settings → Service Accounts → Generate New Private Key
# FIREBASE_SERVICE_ACCOUNT_JSON='{"type":"service_account","project_id":"...","private_key":"...","client_email":"..."}'
#
# METHOD 2: Base64 Encoded Service Account JSON
# --------------------------------------------------------
# Encode your service account JSON to base64 and set as a single variable.
# Create with: cat firebase-service-account.json | base64 -w 0
# FIREBASE_SERVICE_ACCOUNT_BASE64=<base64-encoded-service-account-json>
#
# METHOD 3: Service Account File Path
# --------------------------------------------------------
# Point to a JSON file containing your service account credentials.
# FIREBASE_SERVICE_ACCOUNT_PATH=./firebase-service-account.json
# or
# GOOGLE_APPLICATION_CREDENTIALS=./firebase-service-account.json
#
# METHOD 4 (Lowest Priority): Individual Environment Variables
# --------------------------------------------------------
# Set each credential component as separate environment variables.
# Recommended for development or when using platforms like Vercel.
FIREBASE_PROJECT_ID=your-project-id
FIREBASE_CLIENT_EMAIL=your-service-account-email@your-project-id.iam.gserviceaccount.com
# Note: For FIREBASE_PRIVATE_KEY, you can use either format:
# 1. With literal \n: "-----BEGIN PRIVATE KEY-----\nYour-Private-Key-Here\n-----END PRIVATE KEY-----"
# 2. With actual newlines (multiline string in quotes) - paste directly into Vercel UI
# The system will automatically normalize the key format, including:
#    - Removing extra quotes (including double-wrapped or escaped quotes)
#    - Converting double-escaped newlines (\\n) to proper newlines
#    - Removing empty lines and trimming whitespace
# IMPORTANT: The private key MUST have the BEGIN and END markers on separate lines.
# For Vercel: Simply paste the entire key with newlines directly into the environment variable field.
# The enhanced normalization will handle various encoding formats automatically.
# Example of correct format with literal \n:
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYour-Private-Key-Here\n-----END PRIVATE KEY-----"
FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com
#
# ========================================
# IMPORTANT: Choose ONE method above. If multiple methods are set,
# the system will use the highest priority method (METHOD 1 > METHOD 2 > METHOD 3 > METHOD 4).
# ========================================

# Firebase Authentication (client-side)
NEXT_PUBLIC_FIREBASE_API_KEY=your-firebase-api-key
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-project-id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com
NEXT_PUBLIC_FIREBASE_APP_ID=your-firebase-app-id

# Email Configuration (if using)
RESEND_API_KEY=your-resend-api-key

# Application URL (for social sharing)
NEXT_PUBLIC_APP_URL=https://thegatheringcall.com

# Stripe Configuration
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key

# Add other environment variables as needed
